import { TMDCover } from "@/components";

export default function TMDPage() {
    return (
        <main className="mt-12 mb-14 lg:my-0 grow lg:min-h-svh flex flex-col justify-start items-start p-4">
            <TMDCover/>
        </main>
    );
}